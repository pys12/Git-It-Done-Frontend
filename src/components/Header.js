import React from 'react'
import Login from './Login';
import Logout from './Logout';

const Header = () => {
    return (
        <div>
            <Login />
            <Logout />
        </div>
    )
}

export default Header
